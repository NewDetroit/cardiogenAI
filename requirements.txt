qiskit>=1.0
qiskit-machine-learning>=0.7
qiskit-algorithms>=0.3
qiskit-aer>=0.14
scikit-learn>=1.3
umap-learn>=0.5
transformers>=4.35
torch>=2.0
numpy>=1.24
scipy>=1.10

# Optional: only needed if you want to (re)validate / canonicalise SMILES.
# The shipped dataset already ships RDKit-canonical SMILES, so the pipeline
# itself does not import rdkit at runtime.
# rdkit>=2023.9
